import "./CartWidget.scss"

const CartWidget = () => {
    return(
        <nav className="nav">
            <a className="nav-link">Pasillo</a>
            <a className="nav-link">Pedidos</a>
            <a className="nav-link">Perfil</a>
        </nav>
    )
}
export default CartWidget